// Plain JS with JSDoc — ts-node-pack should copy this verbatim, and
// tsc should emit a .d.ts beside it via the JSDoc pipeline.

/** @returns {number} */
export function plainHelper() {
  return 1;
}
